export const getToken = (state) => {
    return state.token;
}

export const getHospital = (state) => {

    return state.hospitalID;
}

export const getBackLink = (state) => {

    return state.backLink;
}